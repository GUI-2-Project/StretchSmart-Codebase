// Algo for sorting stretches for a stretch routine

// inputs:
    // array of possible stretches
    // questionnair answers
    // user's liked && disliked stretches
        // if stretch ID in disliked stretches, move to back of array
        // if stretch ID in liked stretches, move to front of array

// outputs:
    // sorted array of stretches

// seed random number generator